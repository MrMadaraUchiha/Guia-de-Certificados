// Neste desafio, foi editado uma array para salvar uma lista de palavras usando código. Adicione à array para dar à função pickRandom() mais palavras para escolher.

var answer = pickRandom([ // var answer recebe a função pickRandom
    'yes',                // dentro da função pickRandom tem uma array com três strings.
    'no',
    'sei la',
  ]);
  print(answer); // imprime aleatóriamente uma das três strings.
