// Neste desafio, foi verificado uma propriedade objeto como parte de um teste if statement. Se essa propriedade for maior que > 200, então o objeto será usado na drawBox().

print(rgbObject.blue);
if (rgbObject.blue > 200) {
    drawBox(rgbObject);
}