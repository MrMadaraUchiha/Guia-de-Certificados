// Neste desafio, foi usado uma variável para armazenar dados e, em seguida, foi alterado o valor dessa variável. Por exemplo, var insect = 'Grasshopper' salva a string 'Grasshopper' na variável insect.

var stripeColor = white; // Salva cor branca dentro da variável stripeColor.
drawBox(stripeColor); // Desenha uma quadrado contendo um argumento que contém uma variável que contém a cor branca.
drawBox(stripeColor);
drawBox(stripeColor);
newLine();
stripeColor = blue; // Altera o valor da variável stripeColor para azul.
drawBox(stripeColor); // Desenha uma caixa da cor azul.
drawBox(stripeColor);
drawBox(stripeColor);